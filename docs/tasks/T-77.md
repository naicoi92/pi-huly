# T-77 — fix(search + misc): searchFulltext + preview_deletion cascade + tag-category label

| Field      | Value                          |
| ---------- | ------------------------------ |
| Task       | T-77 [M] medium                |
| Issue      | #85                            |
| Blocked-by | (none)                         |
| Blocks     | (none)                         |
| Branch     | `fix/T-77-search-preview-misc` |

## Vấn đề (reality-checker CONFIRMED)

- fulltext_search: $like findAll (suboptimal — no fulltext index/relevance).
- preview_deletion: chỉ count comments+attachments (bỏ sót subIssues/blockedBy/
  relations/reverseBlocks).
- create/update_tag_category: field `title` (thật là `label` — TagCategory.label).

## Fix

### fulltext_search (search.ts)

- Wire `searchFulltext` vào HulyClient interface (optional). WS wrapper cast-access
  PlatformClient (throws if absent → fallback); REST wrapper delegates rest.searchFulltext.
- Handler: prefer searchFulltext (relevance-ranked) → fallback $like substring
  across Issue+ChatMessage domains if unavailable/throws.

### preview_deletion (deletion.ts)

- Add cascade counters: subIssues (findAll Issue attachedTo), reverseBlocks
  (findAll Issue blockedBy._id), inline blockedBy/relations (RelatedDocument[]
  length). Generate warnings cho non-trivial cascades.

### tag-categories (tag-categories.ts)

- Field `title` → `label` (list read + create + update).
- create: add defaults icon/tags/default (trusted createTagCategory).

## Tests

- t77-search-preview-tagcat.test.ts (new, 7 tests): searchFulltext prefer +
  fallback; preview_deletion cascade; tag-category label + defaults.

## Verification

- 644 tests pass (+7)
- lint/typecheck/fmt/md green

## Residual risks

- WS transport PlatformClient may lack searchFulltext → $like fallback (functional
  but no relevance). REST gets real API.
- searchFulltext result shape assumed {docs:[{id,title,description,doc}]} — if
  server returns different shape, mapping may miss fields. Defensive parse.
- Self-host verify deferred T-53.
