# T-49 — fix(fulltext_search): Document class sai runtime (`tracker:class:Document` broken)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md).

## Metadata

| Field | Value |
|---|---|
| **ID** | T-49 |
| **Size** | S |
| **Priority** | high |
| **Blocked by** | (none) — nhưng cần runtime verify (nên pair với T-51 nếu cần server) |
| **Blocks** | (none) |
| **GitHub issue** | [#38](https://github.com/naicoi92/pi-huly/issues/38) |
| **Status** | Done (defensive fix — root cause Document class deferred T-53) |
| **Related** | T-43 rename Document → `tracker:class:Document` (PR #31), nhưng runtime vẫn lỗi |

## Vấn đề

`huly_fulltext_search` fail với `domain not found: tracker:class:Document`.
Tool description nói "Substring search across Huly workspace" nhưng mọi query
đều lỗi. T-42 (PR #33) đã expand search 3-domain (Issue + Document + ChatMessage),
nhưng Document domain dùng class sai runtime.

```ts
// search.ts:85 (sau T-42 + T-43)
searchDomain(tctx.client, DOCUMENT_CLASS, "title", query, limit, "document"),
// DOCUMENT_CLASS = "tracker:class:Document" (T-43 rename, nhưng runtime vẫn lỗi)
```

`searchDomain` catch server reject `$like` → honest error (T-42 đã fix). Nhưng
vì class sai → reject ngay → toàn bộ search fail (Promise.all reject nếu 1
domain throw).

Hypothesis từ report #38 + audit §7: `tracker:class:Document` có thể vẫn sai.
T-43 audit dựa trên npm tarball source map, KHÔNG verify runtime server
(workvps unavailable). DocumentSnapshot cũng UNVERIFIED (audit §7).

## Phương án

1. **Runtime verify** Document class thật (pair với T-51 nếu cần server, hoặc
   inspect `core:class:Class` registry). Các khả năng:
   - Document thuộc package khác (vd `chunter:class:Document`?).
   - Document class đã deprecated, thay bằng class khác.
2. **Cách dùng đúng**: nếu tìm được class đúng → update `_class-refs.ts` +
   `docs/design/11-runtime-audit.md` truth table.
3. **Defensive fallback** (nếu không verify được ngay): catch `domain not found`
   riêng cho Document domain → log warning + tiếp tục search Issue + ChatMessage
   (KHÔNG để 1 domain fail kéo cả Promise.all). User vẫn search được issue/message
   trong khi Document domain pending fix.
4. **Honest tool description**: nếu Document search không work → note rõ trong
   tool description "Document search currently unavailable" thay vì overclaim.

## Acceptance

- [x] `huly_fulltext_search({ query: "<term>" })` không còn fail `domain not found:
  tracker:class:Document` — defensive Promise.allSettled: Document domain reject → warning log,
  Issue + ChatMessage vẫn trả. KHÔNG fix root cause (deferred T-53).
- [x] Defensive: 1 domain fail không kéo cả search fail — Promise.allSettled (search.ts:99). Test: single-domain-fail +
  multi-fail (2 domain) + all-fail.
- [ ] Document class đúng được update trong `_class-refs.ts` + audit truth table — **DEFERRED T-53** (cần runtime
  verify self-host). Defensive fix đủ cho acceptance #1 + #2.
- [x] Test: search issue title vẫn work ngay cả khi Document domain lỗi — test "1 domain reject (Document) → Issue +
  ChatMessage return + warning" (search.test.ts:153).
- [x] Tool description reflects actual capability — "(if available)" + "Some domains may be unavailable — results show
  which succeeded".

## Review trail

- **reality-checker audit**: 5/5 claim confirmed, 1 unclear (runtime Document class) deferred. BUG THẬT defensive.
- **plan review round 1** (code-review-mentor): NEEDS_FIXES (3 🟡 issues). Fixed: all-rejected branch explicit,
  outer-catch role re-defined, checklist 4/5 + 1 deferred.
- **code-review-mentor review round 2**: APPROVE. 2 🟡 suggestions (multi-fail test + structured failedDomains
  assertion) → applied. 1 nit (?? 0 dead code) → removed. 1 nit (catch comment tighten) → applied.
- **reality-checker integrity**: 4/7 confirmed (3 unverifiable no-bash), 0 fake. Toolchain verified inline: typecheck
  ✅, lint 0 errors ✅, fmt clean ✅, 414 tests pass ✅.
