# T-67 — bug(create_*): addCollection + sequence/identifier/rank/number/kind

| Field | Value |
|---|---|
| Task | T-67 [M] critical |
| Issue | #75 |
| Blocked-by | (none) |
| Blocks | (none) |
| Branch | `fix/T-67-create-attacheddoc` |
| Status | done (pending orchestrator merge) |

## Audit (reality-checker self-audit — hard evidence)

- **MilestoneStatus enum** (@hcengineering/tracker@0.7.0 nub store
  types/index.d.ts:104): `Planned=0, InProgress=1, Completed=2, Canceled=3`.
  create_milestone phải pass `0`.
- **TimeReportDayType** (same source:58): `CurrentWorkDay="CurrentWorkDay"`.
- **TypedSpace.type** (core/src/classes.ts:516): required `Ref<SpaceType>`.
- **Issue = AttachedDoc** (trusted issues-write.ts:188): `addCollection` not `createDoc`.
- **sequence race** (trusted issues-write.ts:133): `$inc: { sequence: 1 }` on Project → return sequence → identifier=`${project.identifier}-${sequence}`.
- **NoParent sentinel** (trusted issues-parent.ts:21): `tracker.ids.NoParent` = `""` (empty ref).
- **Issue.kind** (trusted issues-write.ts:168): `tracker.taskTypes.Issue`.
- **Project self-ref space** (trusted projects.ts:183): `spaceRef = projectId` (project is its own space).

Refs pi-huly derives as literals (Huly convention `<plugin>:<category>:<key>`):
- `tracker:taskTypes:Issue` (kind)
- `tracker:ids:ClassingProjectType` (project type)
- `""` (NoParent)

## Scope (3 tools, surgical)

### create_issue (issues-core.ts)
- `$inc sequence` on Project → get returned sequence
- identifier = `${project.identifier}-${sequence}`
- addCollection (not createDoc) with attachedTo=NoParent, attachedToClass=ISSUE_CLASS, collection="subIssues"
- Add fields: number=sequence, kind=tracker:taskTypes:Issue, identifier, rank="", parents=[], subIssues:0, component:null
- DEFERRED (separate tasks): status Ref resolution (T-71), description MarkupBlobRef (T-72), parentIssue hierarchy (T-68)

### create_project (projects.ts)
- space = self-ref (project._id)
- Add: type=`tracker:ids:ClassingProjectType`, members=[accountUuid],
  owners=[accountUuid], sequence:0, private:false, archived:false,
  defaultIssueStatus:"", defaultTimeReportDay:"CurrentWorkDay"
- Idempotent: findOne by identifier first

### create_milestone (milestones.ts)
- status: 0 (MilestoneStatus.Planned numeric) instead of "planned" string

## Verification plan
- TDD: tests first asserting new behavior (sequence inc, identifier format, addCollection call, milestone status=0)
- `pnpm lint && typecheck && fmt && lint:md && vitest run` all green

## Risks
- `tracker:ids:ClassingProjectType` + `tracker:taskTypes:Issue` literals are
  convention-based (unverified runtime). If wrong, create_project/create_issue
  fail runtime → fallback honest-unavailable. Convention matches existing
  `tracker:class:*` refs.
- Self-host verify deferred (T-53).
