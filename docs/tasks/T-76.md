# T-76 — fix(templates): IssueTemplateChild object + defaults + copy fields

| Field      | Value                         |
| ---------- | ----------------------------- |
| Task       | T-76 [M] high                 |
| Issue      | #84                           |
| Blocked-by | T-68 (done)                   |
| Blocks     | (none)                        |
| Branch     | `fix/T-76-templates-children` |

## Vấn đề (reality-checker CONFIRMED)

- add/remove_template_child: $push/$pull raw string (must IssueTemplateChild
  object + replace full children array).
- create_template: thiếu default fields (priority/assignee/component/estimation/
  children/comments).
- create_issue_from_template: chỉ copy title+description (must copy
  priority/assignee/component).

## Fix

### add_template_child

- Build IssueTemplateChild object `{id (generated), title, description?, priority?,
assignee?, component?, estimation?}`.
- Replace full children array `[...existing, child]` (KHÔNG $push).
- New params: title/description/priority/assignee/component/estimation (thay
  single childTemplate string).

### remove_template_child

- Param renamed childTemplate → childId (IssueTemplateChild.id).
- findIndex by id + filter out + replace array (KHÔNG $pull).

### create_template

- Add defaults: priority="no-priority", assignee=null, component=null,
  estimation=0, children=[], comments=0.

### create_issue_from_template

- Copy priority/assignee/component từ template (trước chỉ title+description).

## Deferred

- Recursive child issue creation (createIssue per template.children +
  attachIssueChild T-68) — heavy (needs createIssue logic extraction). Note as
  follow-up. Single-level iteration, NOT deep recursion (schema forbids nested).

## Tests

- issues-templates.test.ts +6: add (object shape, not-found), remove (find-by-id,
  not-found), create_template defaults, create_from copies fields.

## Verification

- 637 tests pass (+6)
- lint/typecheck/fmt green

## Residual risks

- Recursive child creation deferred — template with children creates parent
  issue only (children not instantiated). Follow-up.
- assignee/component in IssueTemplateChild stored as raw strings (domain layer);
  storage layer uses Refs. Resolution email→Person/label→Component deferred
  (needs findPersonByEmailOrName T-71 name-only + component helper).
- Self-host verify deferred T-53.
