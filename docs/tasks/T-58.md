# T-58 — runtime audit: verify + fix ALL class refs (root cause, KHÔNG defensive)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md). Umbrella root
> cause fix cho #39, #43, #55, #62. User yêu cầu "up level cao hơn".

## Metadata

| Field | Value |
|---|---|
| **ID** | T-58 |
| **Size** | L |
| **Priority** | critical |
| **Blocked by** | (resolved — DEEP-AUDIT source map, KHÔNG cần runtime server) |
| **Blocks** | T-59, T-60 (unblocked) |
| **GitHub issues** | [#39](https://github.com/naicoi92/pi-huly/issues/39), [#43](https://github.com/naicoi92/pi-huly/issues/43), [#55](https://github.com/naicoi92/pi-huly/issues/55), [#62](https://github.com/naicoi92/pi-huly/issues/62) |
| **Status** | Done (DEEP-AUDIT resolved all 5 class refs) |

## Vấn đề

5 class ref trong `_class-refs.ts` ở trạng thái UNVERIFIED/SAI runtime. Bug
report nhiều lần (#39, #43, #55) nhưng fix trước chỉ defensive (T-49
Promise.allSettled che warning, T-43 rename source map mà runtime vẫn fail).
User nhấn mạnh: KHÔNG defensive, verify runtime thật + fix đúng.

## Phương án (DEEP-AUDIT — KHÔNG cần runtime server)

Audit T-44 trước chỉ check **interface existence** trong source map — KHÔNG đủ.
`tracker:class:Document` interface exists NHƯNG runtime KHÔNG register → fail.
T-58 check **plugin() class block** (registration truth) — runtime contract.

### Method

DEEP-AUDIT 12 packages @hcengineering/*@0.7.423: core, tracker, task, contact,
chunter, tags, attachment, drive, view, platform, calendar, templates. Scan:
1. plugin() class block (registration truth)
2. interface existence (TS source)
3. Mutation pattern ($push/$pull/addCollection)

### Verdict (5 class refs — STRONG evidence)

| Class ref | Verdict | Evidence | Action |
|---|---|---|---|
| `tracker:class:Document` | **interface orphan** — interface exists (tracker/src/index.ts:338) NHƯNG KHÔNG register trong tracker plugin() class block (chỉ Project/Issue/IssueTemplate/Component/IssueStatus/Milestone/TimeSpendReport) | plugin() class block scan | T-60: honest-unavailable 5 doc CRUD + search domain remove + link/unlink |
| `core:class:TsRelation` | **KHÔNG TỒN TẠI** — 0 match toàn packages. Issue relations **INLINE** (`relations?: RelatedDocument[]` + `blockedBy?: RelatedDocument[]`) | tracker/src/index.ts:195-196 + core/classes.ts:777 | T-59: refactor $push/$pull inline, xóa TS_RELATION_CLASS |
| `view:class:Label` | **DEPRECATED** — 0 match toàn packages | plugin() class block scan 12 packages | T-58: 4 label CRUD honest-unavailable + issues-core switch TAG_CLASS |
| `document:class:DocumentSnapshot` | **DEPRECATED** — 0 match toàn packages | plugin() class block scan | T-58: 2 snapshot tools honest-unavailable |
| `core:class:Space` (Teamspace) | **base abstract** — KHÔNG có SpaceTypeDescriptor. Documents Teamspace thật = `drive:class:Drive` (extends TypedSpace, registered) | core SpaceTypeDescriptor + drive plugin:31 | T-54: create_teamspace honest-unavailable (SpaceType ref inaccessible) |

## Files changed

- `src/tools/domains/_class-refs.ts` — xóa TS_RELATION_CLASS, thêm DRIVE_CLASS, update comments
- `src/tools/domains/labels.ts` — 4 tool honest-unavailable (redirect tag tools)
- `src/tools/domains/document-snapshots.ts` — 2 tool honest-unavailable
- `src/tools/domains/issues-core.ts` — add/remove_issue_label LABEL_CLASS → TAG_CLASS
- `docs/design/11-runtime-audit.md` §7 — update truth table (RESOLVED 4/5)
- `src/tools/domains/__tests__/class-refs-audit.test.ts` — update verdicts

## Acceptance criteria

- [x] DEEP-AUDIT 12 packages (plugin() class block scan)
- [x] Document: verdict interface orphan (interface exists, NOT registered)
- [x] TsRelation: verdict KHÔNG tồn tại (inline RelatedDocument[])
- [x] Label: verdict deprecated (0 match, dùng TagElement)
- [x] DocumentSnapshot: verdict deprecated (0 match)
- [x] Space/Teamspace: verdict base abstract (Drive là Documents space thật)
- [x] Reality-checker CONFIRMED — KHÔNG còn dead class runtime call
