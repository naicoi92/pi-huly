# T-73 — fix(workflow): create_issue_status + create_task_type register workflow

| Field | Value |
|---|---|
| Task | T-73 [M] high |
| Issue | #81 |
| Blocked-by | (none) |
| Blocks | (none) |
| Branch | `fix/T-73-workflow-registration` |

## Vấn đề (reality-checker CONFIRMED)

- create_issue_status: hardcoded ISSUE_STATUS_CLASS (must dynamic statusClass),
  workspace root space (must core.space.Model), raw category string (must
  Ref<StatusCategory>), KHÔNG register workflow → status orphan (silent fail).
- create_task_type: field ofProjectType (must parent), workspace root space,
  KHÔNG register projectType.tasks.
- Bonus: list_tags thiếu targetClass filter; list_task_types sai field; list_space_types/
  get_space_type fabricated data.

## Fix

### _class-refs.ts
- `MODEL_SPACE = spaceRef("core:space:Model")` — root model space for workflow docs.
- `STATUS_CATEGORY_REFS` map (5 values `task:statusCategory:<Key>`).

### task-management.ts
- **create_issue_status**: full flow — resolve taskType → statusClass dynamic +
  parent projectType; createDoc statusClass MODEL_SPACE {name, ofTaskType,
  category: Ref}; register TaskType.statuses + ProjectType.statuses (ProjectStatus
  objects {_id, taskType}). Idempotent per taskType+name. taskType now REQUIRED.
- **create_task_type**: field parent (KHÔNG ofProjectType) + MODEL_SPACE + register
  projectType.tasks. NOTE: Mixin/TaskTypeClass doc + descriptor template DEFERRED
  (heavy, needs descriptor model — minimal usable TaskType created).
- **list_task_types**: query field `parent` (KHÔNG ofProjectType).

### tags.ts
- **list_tags**: optional targetClass param + filter (tags scoped theo attach class).

### spaces.ts
- **list_space_types / get_space_type**: honest-unavailable (fabricated data
  removed). SpaceTypeDescriptor config từ drive plugin (KHÔNG bundled).

## Tests
- task-management.test.ts (new, 6 tests): create_issue_status full flow + not-found
  and idempotent; create_task_type parent and register and not-found; list_task_types field.
- domains.test.ts: update create_issue_status idempotent test (taskType required).

## Verification
- 630 tests pass (+6 new)
- lint/typecheck/fmt/md green

## Residual risks
- create_task_type Mixin + TaskTypeClass doc + descriptor template DEFERRED —
  minimal TaskType created (usable but not full parity). Follow-up if UI workflow
  editor needs descriptor fields.
- create_issue_status ofAttribute (tracker.attribute.IssueStatus) DEFERRED —
  may not surface in UI workflow editor.
- Self-host verify deferred T-53.
