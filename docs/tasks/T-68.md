# T-68 — fix(issue hierarchy): move_issue + list_issues dùng AttachedDoc fields

| Field      | Value                      |
| ---------- | -------------------------- |
| Task       | T-68 [M] critical          |
| Issue      | #76                        |
| Blocked-by | (none)                     |
| Blocks     | T-76                       |
| Branch     | `fix/T-68-issue-hierarchy` |

## Vấn đề (reality-checker CONFIRMED)

Field `parentIssue` KHÔNG tồn tại runtime trên Issue. Huly models hierarchy qua
AttachedDoc fields: `attachedTo`/`attachedToClass`/`collection`/`parents` +
`subIssues` counter trên parent.

Bug locations (issues-core.ts):

- `list_issues:65` — `query.parentIssue = params.parentIssue` (raw string, field sai)
- `move_issue:468,487` — `parentIssue: null` / `parentIssue: parent._id` (field sai)

**Spec correction**: TASKS.md T-68 ghi `list_issue_relations` — SAI. Bug ở
`list_issues` + `move_issue` (issues-core.ts). `list_issue_relations`
(issues-relations.ts, T-59/T-61) ĐÚNG — KHÔNG đụng.

## Fix plan

### Helper đã có sẵn: `issues-hierarchy.ts` (untracked, verify OK)

- `topLevelIssueParent()` → {attachedTo:NoParent, attachedToClass:Issue, collection:"subIssues", parents:[]}
- `childIssueParent(parent, space)` → set attachedTo=parent._id + parents breadcrumb
- `hasConcreteIssueParent(issue)` → check có parent thật
- `attachIssueChild(client, space, childId, parent, extraOps)` → updateDoc child + $inc parent subIssues
- `updateDescendantParents(client, space, parent, newParents)` → recursive breadcrumb

### list_issues (issues-core.ts:65)

- params.parentIssue → resolve identifier → _id (findOne Issue by identifier)
- `query.attachedTo = _id` (thay query.parentIssue)
- parentIssue not found → isError

### move_issue (issues-core.ts:434-494) — rewrite

1. Find issue by identifier (giữ).
2. Capture old parent state: `oldAttachedTo = issue.attachedTo`, `wasChild = hasConcreteIssueParent(issue)`.
3. Top-level (no parentIssue param):
   - updateDoc issue với `topLevelIssueParent()` fields
   - nếu wasChild → dec old parent subIssues ($inc -1)
   - nếu issue.subIssues > 0 → updateDescendantParents(issue, [])
4. Child (parentIssue param):
   - find parent by identifier (giữ validate)
   - `attachIssueChild(client, projectSpace, issue._id, parent, {})` — set fields + $inc parent +1
   - nếu wasChild → dec old parent subIssues ($inc -1) — **LUÔN dec khi
     wasChild** (KHÔNG guard old==new; same-parent: attachIssueChild +1 + dec
     -1 = net 0, match trusted issues-move.ts:103-110)
   - nếu issue.subIssues > 0 → updateDescendantParents(issue, parent.parents + parentInfo)
5. Return: `parentIssue: <identifier or null>` (computed, không raw field)

### create_issue — DEFERRED

parentIssue param vẫn ignored (T-67 deferred). T-68 scope = move + list only.
Direct sub-issue creation = follow-up (note trong handoff).

### Tests (issues-core.test.ts:614-680 rewrite)

- move top-level: assert updateDoc ops = topLevelIssueParent fields (KHÔNG parentIssue)
- move child: assert attachIssueChild gọi (2 updateDoc: child fields + parent $inc)
- move child was-child: assert 3 updateDoc (child, new parent +1, old parent -1)
- move descendants: assert updateDescendantParents recursive
- list parentIssue filter: assert query.attachedTo = resolved _id

## Constraints

- Surgical: chỉ list_issues + move_issue + helper wire. KHÔNG refactor relations.
- Pi-huly string-literal class refs (ISSUE_CLASS, NO_PARENT_REF đã có).
- Non-atomic multi-updateDoc (khớp trusted, acceptable — note residual risk).
- Verify: lint + typecheck + fmt + lint:md + vitest green.

## Residual risks

- Non-atomic move (set + 2× $inc + N descendants) — failure mid-sequence desync subIssues. Trusted cũng vậy, accepted.
- create sub-issue still deferred.
- Self-host verify deferred (T-53).
