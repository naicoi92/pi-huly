# T-54 — fix(create_teamspace): honest-unavailable (base class Space abstract)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md). Pair với T-58
> (runtime audit) khi user cung cấp self-host probe output.

## Metadata

| Field | Value |
|---|---|
| **ID** | T-54 |
| **Size** | M |
| **Priority** | high |
| **Blocked by** | (none code — T-58 pair nếu cần verify class) |
| **Blocks** | (none) |
| **GitHub issue** | [#58](https://github.com/naicoi92/pi-huly/issues/58) |
| **Status** | Done (honest-unavailable) |

## Vấn đề

`huly_create_teamspace` fail với `platform:status:UnknownError`. Root cause giống
issue #28 (create_todo đã fix T-46 — thiếu required fields), NHƯNG có thêm vấn đề
**class sai runtime**:

1. **Sai class**: hiện dùng `SPACE_CLASS` (`core:class:Space`). Reality-checker
   audit STRONG (npm tarball source map @0.7.423, 8 packages) confirm đây là
   **base abstract class KHÔNG có SpaceTypeDescriptor** → createDoc tạo space
   vô hình (UI không hiển thị, không permission model).
2. **KHÔNG có class "Teamspace" runtime** — "Teamspace" là UI label only. User
   spaces phải đi qua TypedSpace subclass:
   - `tracker:class:Project` (Issues space)
   - `drive:class:Drive` (Documents/Files space, `interface Drive extends TypedSpace {}`)
   - `chunter:class:ChunterSpace` (Chat space)
3. **Thiếu required fields**: TypedSpace subclass cần `attachedToClass`, `user`,
   `visibility`, `rank`, `type` (Ref<SpaceType>) — code hiện tại KHÔNG fill.

## Phương án (honest-unavailable — KHÔNG đoán class)

Pi-huly KHÔNG đoán class mới (sai loại space = hậu quả tệ hơn fail rõ ràng).
Honest-unavailable cho đến khi T-58 verify class Teamspace thật runtime:

- `create_teamspace` handler return `isError: true` ngay tại entry, KHÔNG gọi
  `createDoc` (tránh tạo space rác vô hình).
- Error message explain root cause + recovery path (tạo qua Huly UI trực tiếp,
  UI chọn đúng TypedSpace subclass theo SpaceType).
- `details: { reason: "class_unverified", blockedBy: "T-58" }` cho debug.
- Tool description flag `UNAVAILABLE` rõ cho LLM.

Read path (list/get/update/delete_teamspace) vẫn OK qua `SPACE_CLASS` — findAll
trên base class trả subclasses qua inheritance.

## Files changed

- `src/tools/domains/documents.ts` — `create_teamspace` handler honest-unavailable
- `src/tools/domains/_class-refs.ts` — comment SPACE_CLASS base abstract
- `src/tools/domains/__tests__/documents.test.ts` — 5 tests (3 create + 2 read regression)

## Acceptance criteria

- [x] Verify class thật của Teamspace runtime (reality-checker STRONG: base abstract)
- [x] KHÔNG đoán class — honest-unavailable thay vì createDoc class sai
- [x] Tool description flag UNAVAILABLE rõ cho LLM
- [x] Regression test: mock client verify `createDoc` KHÔNG được gọi
- [x] Read path (list/get_teamspaces) regression guard — vẫn OK
- [ ] Manual verify runtime — pending T-58 (re-enable khi class verify)

## Re-enable khi T-58 verify

Khi T-58 xác định class Teamspace thật runtime, revert handler sang `createDoc`:
1. Update `SPACE_CLASS` hoặc thêm `TEAMSPACE_CLASS` constant với class đúng.
2. Fill required fields theo pattern T-46 (user/visibility/rank/type).
3. Wrap error honest (pair T-57 — UnavailableError cho domain not found).
4. Update test: verify createDoc được gọi với đầy đủ required fields.
