# T-71 — fix(list_*): space scoping + assignee resolve + list_statuses ProjectType

| Field      | Value                   |
| ---------- | ----------------------- |
| Task       | T-71 [M] high           |
| Issue      | #79                     |
| Blocked-by | (none)                  |
| Blocks     | (none)                  |
| Branch     | `fix/T-71-list-scoping` |

## Vấn đề (reality-checker CONFIRMED)

5 list_* tool thiếu space scoping → trả cross-project:

- list_issues: pseudo-scope `identifier $like` (fragile, leak khi titleSearch)
- list_milestones/components/templates: `findAll(CLASS, {})` global
- list_statuses: `findAll(ISSUE_STATUS_CLASS, {})` global (KHÔNG theo project)
- list_issues assignee raw email (Issue.assignee = Ref<Person>, silent zero-result)

## Fix

### Helpers (`_common.ts`)

- `getProjectSpace(client, identifier)` → project._id (= space, self-ref T-67)
- `getProjectStatuses(client, identifier)` → ProjectType.statuses traversal:
  Project → project.type → ProjectType.statuses Ref[] → resolve IssueStatus docs
  - category ref→enum (strip `*:statusCategory:` prefix) + isDefault

### Helper (`contacts.ts`)

- `findPersonByEmailOrName(client, input)` → Person._id (email match first,
  fallback name). Port trusted contacts-shared.ts:190.

### Tool fixes (5)

- list_issues: replace `identifier $like` → `space: project._id`; assignee
  resolve via findPersonByEmailOrName; titleSearch ADD filter (KHÔNG xóa space)
- list_milestones/components/templates: `findAll(CLASS, { space })`
- list_statuses: getProjectStatuses (ProjectType traversal + category enum +
  isDefault)

### Tests

- issues-core.test.ts: +3 T-71 tests (space scoping, project not-found,
  titleSearch no-leak) + update T-68 parentIssue tests (project seed)
- list-scoping.test.ts (new): 7 tests cover milestones/components/templates
  space scoping + list_statuses ProjectType traversal (happy/not-found/no-type)

## Verification

- 620 tests pass (+10)
- lint/typecheck/fmt/md green

## Residual risks

- list_issues `statusCategory` filter still raw enum (IssueStatus has `category`
  Ref not `statusCategory` field — separate latent bug, deferred).
- assignee resolve: chỉ name match (Person.email field KHÔNG tồn tại — email
  needs Channel(Email) lookup, deferred follow-up). Caller truyền email → isError.
- ProjectType.statuses = ProjectStatus[] (objects {_id, taskType}) — extract
  ._id trước resolve (reviewer B1 catch).
- Self-host verify deferred T-53.
