# T-70 — fix(comments): field `message` (inline Markup), KHÔNG `body`

| Field | Value |
|---|---|
| Task | T-70 [M] critical |
| Issue | #78 |
| Blocked-by | (none) |
| Blocks | (none) |
| Branch | `fix/T-70-comments-field-message` |

## Vấn đề (reality-checker CONFIRMED)

Pi-huly comments domain dùng field `body` — Huly field thật là `message`. Comment
luôn rỗng (add silent), update silent no-op, list trả undefined. Header comment
- design doc `05-data-model.md:122` encode gotcha ĐẢO ngược (`body /*KHÔNG message*/`).

Verified vs trusted `comments.ts:150`: `message: markdownToMarkupString(params.body)`.

## Fix

### `message` = inline Markup (KHÔNG MarkupBlobRef, KHÔNG uploadMarkup)
ChatMessage.message type = inline Huly Markup = `JSON.stringify(markupNode)`.
Pi-huly đã có `mdToMarkup` (markup/markup.ts) — same pattern issues-core/todos.
Helper `markdownToMessage(md) = JSON.stringify(mdToMarkup(md))` + round-trip
`messageToMarkdown` cho list read.

### comments.ts changes (3 tools)
- **add_comment**: `{ body: params.body }` → `{ message: markdownToMessage(body) }`
- **update_comment**: `{ body: ... }` → `{ message: ..., editedOn: Date.now() }`
- **list_comments**: read `c.message` (markupToMd round-trip) + thêm filter
  `attachedToClass: ISSUE_CLASS` + sort `{createdOn: 1}` (trusted pattern)
- Header + add_comment description: xóa inverted gotcha

### design doc fix
`05-data-model.md:122`: `body: string /*KHÔNG "message"*/` → `message: string /*inline Markup, KHÔNG "body" — T-70 fix*/`

### tests (new file — zero coverage trước)
`__tests__/comments.test.ts`: 7 tests cover add/update/list × happy + not-found.
Assert `message` field + `attachedToClass` filter + markup round-trip.

## Verification
- 608 tests pass (+7 new comments tests)
- lint/typecheck/fmt/md green

## Residual risks
- `messageToMarkdown` JSON.parse fallback raw string cho legacy non-JSON (edge).
- create `addCollection` space = `issue.space` (trusted dùng `project._id` —
  equivalent, Issue.space === Project._id). Non-blocking.
- markupToMd round-trip cho complex markup (native refs) có thể lossy — R8
  covered bởi markup module, acceptable v1.
