# T-69 — fix(tags): TagReference AttachedDoc (collection "labels"), KHÔNG $push/$pull inline

| Field      | Value                        |
| ---------- | ---------------------------- |
| Task       | T-69 [M] high                |
| Issue      | #77                          |
| Blocked-by | (none)                       |
| Blocks     | (none)                       |
| Branch     | `fix/T-69-tags-tagreference` |

## Vấn đề (reality-checker CONFIRMED + bonus blocker)

Pi-huly tags dùng `$push`/`$pull` inline `issue.tags` array + field name `"tags"` —
SAI cả 2:

1. Huly dùng `TagReference` AttachedDoc (addCollection/findAll/removeDoc), KHÔNG inline.
2. Collection field thật = `"labels"` (KHÔNG `"tags"`) — Issue.labels: TagReference[].

Verified vs trusted `tags-shared.ts:297` `attachTagReference` + canonical
`tags/types/index.d.ts:32` TagReference interface.

## Fix

### `_class-refs.ts`: thêm TAG_REFERENCE_CLASS

`export const TAG_REFERENCE_CLASS = classRef("tags:class:TagReference");`

### tags.ts (3 tools rewrite)

- **attach_tag**: `$push {tags}` → `addCollection(TAG_REFERENCE_CLASS, issue.space,
issue._id, ISSUE_CLASS, "labels", {tag, title, color:Number, weight?})`.
  Idempotent via `findAll TagReference {tag}` check.
- **detach_tag**: `$pull` → `findAll TagReference {attachedTo, collection:"labels",
tag}` + `removeDoc` each matching.
- **list_attached_tags**: `issue.tags` inline read → `findAll(TAG_REFERENCE_CLASS,
{attachedTo, attachedToClass, collection:"labels"})`.
- color coerce `Number(tagDoc.color ?? 0)` (canonical color = number).

### Tests (tags.test.ts rewrite)

- Replace T-52 $push/$pull asserts với T-69 addCollection/findAll/removeDoc asserts.
- 9 tests cover attach (happy/idempotent/not-found) + detach (happy/idempotent) +
  list (happy/not-found) + T-63 schema drift guard (preserved).

## Scope decision (spec/API auto-resolve)

Spec T-69 đề xuất "generic theo targetClass param (không hardcoded Issue)". Deviation:
giữ Issue-scoped UX (ponytail — generic objectClass/space/collection là scope
widening, đổi API contract 102-tool). Fix data model + collection field = core bug.
Generic = deferred follow-up (note).

## Residual risks

- color string/number cross-cutting (create_tag/list_tag vẫn string) — deferred,
  T-69 chỉ coerce tại attach boundary.
- weight param chưa expose (type 0-8 union) — optional, omit khi undefined (match trusted).
- Generic targetClass deferred (note ở trên).
- Self-host verify deferred T-53.
