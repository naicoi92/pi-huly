# T-42 — fix(fulltext_search): real search thay `$like` stub

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md).

## Metadata

| Field | Value |
|---|---|
| **ID** | T-42 |
| **Size** | M |
| **Priority** | high |
| **Blocked by** | T-44 (cần verify `$like` operator có work + Huly có search endpoint thật không) |
| **Blocks** | (none) |
| **GitHub issue** | [#24](https://github.com/naicoi92/pi-huly/issues/24) |
| **Status** | Open |

## Vấn đề

`src/tools/domains/search.ts:24-31` — stub chỉ `findAll Issue { title: {$like} }`,
bỏ description/document/message. Ngay cả title exact substring match cũng 0 →
`$like` operator hoặc escape function sai, hoặc Huly server không support `$like`.

```ts
// Huly search API: client.findOne/findAll không support fulltext trực tiếp.
// Real implementation dùng dedicated search endpoint (api-client export).
// Simplified: query title/description substring cho issues.
// TODO: replace bằng real search API khi Huly expose.
const issues = await tctx.client.findAll(
  "tracker:class:Issue" as never,
  { title: { $like: `%${escapeLikePattern(params.query)}%` } },
  { limit },
);
```

2 vấn đề:

1. **TODO chưa làm**: chỉ search title, bỏ description/document/message dù
   tool description claim "search across issues, documents, messages".
2. **`$like` không work** (hoặc escape/function sai): exact substring title
   match vẫn 0. Cần verify `$like` operator có được Huly server support hay
   không, hoặc cú pháp query sai.

## Phương án (sau T-44 verify)

1. **Fix `$like`** nếu operator đúng nhưng syntax/escape sai (test riêng).
2. **Implement real search** nếu Huly expose search endpoint
   (`@hcengineering/search` hoặc query protocol khác). Expand query thủ công
   nếu cần:
   `findAll Issue { $or: [{title:{$like}}, {description:{$like}}] }` +
   `findAll Document { content: {$like} }` + ChatMessage.
3. **Honestly document** nếu Huly KHÔNG expose fulltext: đổi tool `description`
   + `promptSnippet` thành "Title substring search only" để LLM không
   fake-confident.

## Acceptance

+ [ ] Search exact substring trong title → match (reproduce LST-1 "Critical Runtime Wiring")
+ [ ] Search description + document content + message (nếu Huly support)
+ [ ] Test: verify `$like` operator behavior thật; nếu không work, dùng approach khác + document
+ [ ] Tool `description` + `promptSnippet` reflects actual capability (không overclaim)
