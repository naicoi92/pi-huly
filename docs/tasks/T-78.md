# T-78 — fix(teamspace): implement create_teamspace (string-literal icon/spaceType refs)

| Field | Value |
|---|---|
| Task | T-78 [M] high |
| Issue | (to-create) |
| Blocked-by | (none) |
| Blocks | (none) |
| Branch | `fix/T-78-create-teamspace` |
| Reporter | user (blocker: Huly docs workflow) |

## Vấn đề

User báo blocker:
- `huly_create_teamspace` ❌ UNAVAILABLE (T-66 conclusion: "needs documentPlugin refs, not bundled")
- `huly_list_teamspaces` chỉ trả count, KHÔNG expose teamspace ID
- → KHÔNG tự create/resolve teamspace được → block document workflow

T-66 conclusion **SAI**: claimed icon/spaceType refs cần bundle document plugin.

## Root cause (reality-checker verify runtime)

Plugin refs = **plain string literals** (plugin() factory prefixes `<pluginId>:<category>:<name>`).
Verified via `node -e` trên trusted's installed `@hcengineering/document`:
```
documentPlugin.class.Teamspace             = "document:class:Teamspace"
documentPlugin.icon.Teamspace              = "document:icon:Teamspace"
documentPlugin.spaceType.DefaultTeamspaceType = "document:spaceType:DefaultTeamspaceType"
```
→ Same T-65 string-literal pattern. KHÔNG cần bundle plugin.

## Fix

### _class-refs.ts
- Add `TEAMSPACE_ICON = "document:icon:Teamspace"`
- Add `DEFAULT_TEAMSPACE_TYPE = "document:spaceType:DefaultTeamspaceType"`
- Add `SPACE_PARENT = spaceRef("core:space:Space")` (fix latent bug: old
  `spaceRef("core.space.Space")` dùng DOT separator — sai, phải colon).
- T-66 update/delete_teamspace dùng SPACE_PARENT → giờ đúng colon form.

### documents.ts create_teamspace
- Idempotent: findOne(TEAMSPACE_CLASS, {name, archived:false}) → return existing {id, created:false}.
- createDoc(TEAMSPACE_CLASS, SPACE_PARENT, {name, description:"", private:false,
  archived:false, members:[uuid], owners:[uuid], icon, type}).
- uuid từ getAccount().uuid.
- Return {id, name, created:true}.

### documents.ts list_teamspaces
- T-78: content message surface ids+names (trước chỉ "Found N teamspace(s)").
  Giờ: `Found N teamspace(s):\n- Name (id)` → agent đọc content resolve được.

## Tests
- documents.test.ts: replace 3 T-66 unavailable tests → 2 T-78 create tests
  (create attrs verify icon/type/members/owners + idempotent existing).
- Fix 2 pre-existing update/delete tests: colon form `core:space:Space`.

## Verification
- 650 tests pass (net -1: 3 unavailable → 2 create)
- lint/typecheck/fmt/md green

## Residual risks
- Self-host runtime verify deferred T-53 (refs verified via node eval on trusted install).
- getAccount().uuid: ws + rest both wired (client.ts getAccount delegate).
