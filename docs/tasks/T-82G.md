# T-82G — enhancement(milestones/workspace/contacts): fields + email resolve + sort/clear

| Field      | Value                                                          |
| ---------- | -------------------------------------------------------------- |
| Task       | T-82G [M] low                                                  |
| Issue      | #108                                                           |
| Blocked-by | T-82 (same domain — fix bug trước)                             |
| Blocks     | T-80 (findPersonByEmailOrName unblocks assignee email resolve) |
| Branch     | `enh/T-82G-ms-ws-contacts-completeness`                        |

## Vấn đề

Tools chạy đúng (sau T-82 fix bug) nhưng thiếu fields/resolution. 5 gaps — KHÔNG
break (workspace tools honest-unavailable = by design T-74 ADR, KHÔNG phải gap).

### Gaps (vs trusted)

- **get_milestone**: thiếu `description` (markdown), `project`, `modifiedOn`/`createdOn`
- **findPersonByEmailOrName**: email path KHÔNG implement (chỉ name-match) — comment
  ghi rõ deferred. Trusted: 5-step resolution (SocialIdentity → Channel exact → name
  exact → Channel $like → name $like). Hiện caller truyền email → undefined → isError
  (assignee lookup fail).
- **list_milestones**: thiếu sort `{ modifiedOn: Descending }`
- **list_persons**/`list_employees`: thiếu fields phụ (`city`/`modifiedOn` cho persons;
  `position`/`active` cho employees)
- **set_issue_milestone**: không support clear (`milestone: null`)

### Note

Workspace tools (`list_workspaces`, `list_workspace_members`) honest-unavailable =
by design (T-74 ADR account-client sub-slice 2 deferred) — KHÔNG phải gap để file.

## Fix

### `milestones.ts`

- get_milestone: add description (markdown fetchMarkup), project, modifiedOn, createdOn
- list_milestones: add sort `{ modifiedOn: Descending }` vào findAll options
- set_issue_milestone: `milestone: Type.Union([Type.String(), Type.Null()])` + null clear

### `contacts.ts`

- **findPersonByEmailOrName** (HIGH VALUE — unblocks T-80): implement email resolve.
  Port trusted 5-step HOẶC tối thiểu Channel provider Email exact match:
  `findOne(CHANNEL_CLASS, { attachedTo: person._id, provider: "email", value: email })`
  → resolve Person. If no Channel match → name fallback.
- list_persons: add city, modifiedOn
- list_employees: add position, active

## Tests

- get_milestone: description/project/modifiedOn/createdOn
- findPersonByEmailOrName: email via Channel resolve (mock Channel) + name fallback
- list_milestones: sort order
- set_issue_milestone: null clear
- list_persons/employees: new fields

## Verification

- Tests pass (target +~8), lint/typecheck/fmt/md green, CI green
- **Cross-verify**: T-80 assignee resolve work với email input sau T-82G

## Residual risks

- Channel class ref + provider field cần verify (contact:class:Channel, provider:"email")
- SocialIdentity $like fallback = best-effort (perf) — defer nếu Channel exact đủ
- This task unblocks T-80 — coordinate order (T-82G trước T-80 nếu assignee email needed)
