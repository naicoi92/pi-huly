# T-60 — fix(fulltext_search): Document search root cause (interface orphan)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md). T-58 verify
> `tracker:class:Document` interface orphan — NOT registered runtime.

## Metadata

| Field | Value |
|---|---|
| **ID** | T-60 |
| **Size** | M |
| **Priority** | high |
| **Blocked by** | T-58 (verify Document class runtime — interface orphan) |
| **Blocks** | (none) |
| **GitHub issues** | [#55](https://github.com/naicoi92/pi-huly/issues/55), [#64](https://github.com/naicoi92/pi-huly/issues/64) |
| **Status** | Done |

## Vấn đề

`huly_fulltext_search` Document domain fail `domain not found: tracker:class:Document`
(#55 report 2 lần). T-43 rename `document:class:Document` → `tracker:class:Document`
dựa source map NHƯNG runtime vẫn fail. T-49 thêm Promise.allSettled → biến lỗi
thành warning (che mắt user) — root cause chưa fix.

## T-58 verdict: Document interface orphan

T-58 DEEP-AUDIT STRONG confirm: `interface Document extends Doc` exists trong
tracker source (src/index.ts:338) NHƯNG **KHÔNG register** trong tracker plugin()
class block (chỉ Project/Issue/IssueTemplate/Component/IssueStatus/Milestone/
TimeSpendReport/RelatedIssueTarget/ProjectTargetPreference).

→ Interface orphan — runtime lookup fail "domain not found". Huly KHÔNG register
Document class trong 12 packages audited @0.7.423.

## Phương án (honest-unavailable — KHÔNG defensive)

User yêu cầu "KHÔNG defensive che lỗi". REMOVE Document domain khỏi search +
honest-unavailable tất cả Document CRUD tools (cùng verdict root cause):

1. **search.ts**: REMOVE Document domain, chỉ 2 domain (Issue title + ChatMessage
   content). Tool description honest "Document search NOT available".
2. **documents.ts**: 5 tool CRUD (list/get/create/edit/delete_document) →
   honest-unavailable (return isError ngay, KHÔNG query dead class).
3. **issues-relations.ts**: link/unlink_document_to_issue → honest-unavailable
   (cùng DOCUMENT_CLASS orphan).

Total 7 tool honest-unavailable + 1 search domain removed.

## Files changed

- `src/tools/domains/search.ts` — remove Document domain (2 domain only)
- `src/tools/domains/documents.ts` — 5 tool honest-unavailable (interface orphan)
- `src/tools/domains/issues-relations.ts` — link/unlink honest-unavailable
- `src/tools/domains/__tests__/search.test.ts` — update 2 domain + description
- `src/tools/domains/__tests__/documents.test.ts` — 5 honest-unavailable tests
- `src/__tests__/e2e-smoke.test.ts` — update create/edit_document honest-unavailable

## Acceptance criteria

- [x] T-58 verify Document class runtime (interface orphan — NOT registered)
- [x] search.ts REMOVE Document domain (only Issue + ChatMessage)
- [x] 5 document CRUD honest-unavailable (KHÔNG query dead class)
- [x] link/unlink_document_to_issue honest-unavailable
- [x] Tool description honest "Document search NOT available"
- [x] Reality-checker CONFIRMED — 0 DOCUMENT_CLASS runtime call còn sót
- [x] Close #55 (report 2 lần, root cause finally fixed)
