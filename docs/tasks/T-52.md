# T-52 — fix(*): validate foreign-key ref tồn tại trước write (7 tool)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md). Generalization của
> pattern đã làm đúng ở T-45 (`add_issue_label` PR #34) + `remove_issue_relation`.

## Metadata

| Field | Value |
|---|---|
| **ID** | T-52 |
| **Size** | L |
| **Priority** | critical |
| **Blocked by** | (none) — reference pattern đã ship ở PR #34 (T-45) |
| **Blocks** | (none) |
| **GitHub issue** | [#42](https://github.com/naicoi92/pi-huly/issues/42) |
| **Status** | Done |

## Vấn đề

Nhiều tool nhận user input là entity reference (`targetIssue`, `component`,
`milestone`, `parentIssue`, `document`, `tag`) rồi cast trực tiếp thành `Ref<T>`
qua `idRef(params.xxx)` và ghi vào Issue — **không validate** entity đó có tồn tại.
Hệ quả:

1. Issue update thành công về phía tool (lừa LLM).
2. Khi *client khác* (subagent, UI) sync và apply TxUpdateDoc cho target document
   không tồn tại → warning `no document found, failed to apply model transaction,
   skipping _class="core:class:TxUpdateDoc"` spam cho mỗi ref rác.
3. Dữ liệu Issue bị ref rác (vd `component: <bogus-id>`), không nhất quán giữa các client.

### Call sites cần sửa (7 tool — verify code)

| Tool | File:line | Param ref | Entity class cần lookup |
|---|---|---|---|
| `add_issue_relation` | `issues-relations.ts:49` | `targetIssue` | `tracker:class:Issue` (theo identifier) |
| `set_issue_component` | `components.ts:158` | `component` | `tracker:class:Component` |
| `set_issue_milestone` | `milestones.ts:179` | `milestone` | `tracker:class:Milestone` |
| `move_issue` | `issues-core.ts:336` | `parentIssue` | `tracker:class:Issue` |
| `link_document_to_issue` | `issues-relations.ts:165` | `document` | `tracker:class:Document` |
| `unlink_document_to_issue` | `issues-relations.ts:198` | `document` | `tracker:class:Document` (optional skip — $pull idempotent) |
| `attach_tag` | `tags.ts:190` | `tag` | `tags:class:TagElement` |

## Pattern chuẩn (đã đúng ở `add_issue_label` T-45 PR #34 + `remove_issue_relation`)

```ts
const target = await tctx.client.findOne(TARGET_CLASS, { _id: idRef(params.targetId) });
if (!target) {
  return {
    content: `Target "${params.targetId}" not found.`,
    isError: true,
    details: { target: params.targetId },
  };
}
// chỉ mới gọi updateDoc/addCollection sau khi target verified
```

## Phương án

1. Áp dụng pattern chuẩn cho 7 tool trên — `findOne` target entity trước khi
   `updateDoc`/`addCollection`/`removeDoc`.
2. **`add_issue_relation` đặc biệt**: vì `targetIssue` nhận identifier (`PD-123`)
   chứ không phải raw `_id`, phải resolve identifier → `_id` trước (dùng
   `resolveIdentifier` rồi `findOne(ISSUE_CLASS, { identifier })`).
3. **`unlink_document_to_issue`**: $pull idempotent → optional skip nếu không
   tồn tại (không cần hard error). Nhưng vẫn nên validate để bắt typo sớm.
4. **Code reuse**: extract helper `assertEntityExists(client, _class, id)` ở
   `_common.ts` nếu phù hợp, tránh lặp 7 lần.

## Acceptance

- [x] 6/7 tool `findOne` target entity trước khi write (`unlink_document_to_issue`
  skip per spec §Phương án 3 — $pull idempotent, defer validate)
- [x] Target không tồn tại → `isError: true`, message rõ ràng (pattern như
  `add_issue_label`)
- [x] Regression test cho mỗi tool: mock `findOne(TARGET_CLASS)` trả `undefined` →
  assert `updateDoc`/`addCollection` KHÔNG gọi, tool trả `isError`
- [x] Happy path regression test: target tồn tại → write xảy ra với ref đúng
- [x] `add_issue_relation`: `targetIssue` identifier → `_id` resolve (cross-project
  OK — KHÔNG route resolveIdentifier, query trực tiếp by identifier)
- [ ] (Optional) Extract helper `assertEntityExists` — **DEFERRED** (pattern ở ~10
  sites sau T-52, follow-up extraction)

## Review trail

- **reality-checker audit**: 5/5 claim confirmed. 6/7 tool bug. Recommend extract
  helper (defer).
- **User chốt THIẾT KẾ conflict**: Option A (move_issue KHÔNG truyền = top-level)
  + attach_tag shape fix included.
- **plan review** (code-review-mentor): NEEDS_FIXES (2 🔴 blockers: cross-project
  targetIssue, T-51 test files premise). Fixed: query trực tiếp KHÔNG
  resolveIdentifier, tạo test files mới.
- **Bonus shape fix** (attach_tag): idempotent check raw string vs ref array →
  ref resolved. $push TagReference object `{tag, title, color}` (color string,
  khác Label number T-45).
- **link_document_to_issue**: tách message "Issue or document not found" gộp →
  2 nhánh rõ (issue first, then document).

## Bonus findings (audit + plan review)

- `add_issue_relation` double bug: no validate + identifier cast thành _id
  (`idRef(params.targetIssue)` raw). Fix: resolve identifier → findOne → `_id` thật.
- `move_issue` THIẾT KẾ conflict: description nói "null = top-level" nhưng schema
  Optional<String>. User chốt Option A: KHÔNG truyền = top-level, update cả 2
  descriptions.
- `attach_tag` idempotent shape: `existing.includes(params.tag)` raw string vs ref
  array → luôn false → re-push duplicate (giống T-45). Fix included.
- `link_document_to_issue` message gộp mơ hồ. Fix: tách 2 nhánh.
