# T-79G — enhancement(todos): update fields + schedule/unschedule tools

| Field      | Value                              |
| ---------- | ---------------------------------- |
| Task       | T-79G [M] low                      |
| Issue      | #106                               |
| Blocked-by | T-79 (same domain — fix bug trước) |
| Blocks     | (none)                             |
| Branch     | `enh/T-79G-todos-completeness`     |

## Vấn đề

Todos tools chạy đúng storage (sau T-79 fix bug) nhưng thiếu capability vs trusted.
Trusted có 9 todo tools, pi-huly có 7.

### Gaps (vs trusted `planner.ts`)

- **update_todo** thiếu fields:
  - Chỉ có title/description/dueDate. Thiếu `owner(→user)`, `priority(ToDoPriority)`,
    `visibility`.
  - description raw string (thật Markup ref qua uploadMarkup).
  - KHÔNG support `dueDate: null` ($unset clear).
- **Thiếu 2 tool**:
  - `schedule_todo` (work slot / calendar integration)
  - `unschedule_todo`

## Fix

### `todos.ts` update_todo
- Add fields: owner (resolve Employee via findPersonByEmailOrName → user), priority
  (ToDoPriority map high:0/medium:1/low:2/no-priority:3/urgent:4), visibility
- description qua uploadMarkup (KHÔNG raw string)
- `dueDate: Type.Union([Type.String(), Type.Null()])` + branch null → $unset clear

### `todos.ts` (optional, defer nếu phức tạp)
- Add `schedule_todo` + `unschedule_todo` (work slot creation/removal). Port trusted
  planner.ts scheduleTodo/unscheduleTodo. Cần `workslots` field + calendar integration.

## Tests
- update_todo: owner/priority/visibility fields set đúng
- update_todo: description markup upload
- update_todo: dueDate null → $unset
- (optional) schedule/unschedule_todo round-trip

## Verification
- Tests pass, lint/typecheck/fmt/md green, CI green

## Residual risks
- schedule/unschedule cần verify work slot model (time plugin) — defer nếu out of scope
- owner resolve phụ thuộc findPersonByEmailOrName (T-82G email path)
