# T-59 — refactor(issue_relations): $push/$pull inline (TsRelation KHÔNG class riêng)

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md). T-58 verify
> `TsRelation` KHÔNG tồn tại runtime → Issue relations stored inline.

## Metadata

| Field | Value |
|---|---|
| **ID** | T-59 |
| **Size** | M |
| **Priority** | high |
| **Blocked by** | T-58 (verify TsRelation inline hypothesis — CONFIRMED) |
| **Blocks** | (none) |
| **GitHub issue** | [#63](https://github.com/naicoi92/pi-huly/issues/63) |
| **Status** | Done |

## Vấn đề

`add/remove/list_issue_relation` dùng `addCollection(TS_RELATION_CLASS, ...)` /
`findAll(TS_RELATION_CLASS)` / `removeDoc(TS_RELATION_CLASS)` — class
`core:class:TsRelation` KHÔNG tồn tại runtime (T-58 DEEP-AUDIT 0 match toàn
packages) → tool fail `domain not found` (#39).

## T-58 verdict: Issue relations INLINE

T-58 DEEP-AUDIT confirm (tracker/src/index.ts:195-196):

```typescript
export interface Issue extends Task {
  blockedBy?: RelatedDocument[]   // is-blocked-by direction
  relations?: RelatedDocument[]   // blocks/relates-to direction
}
```

`RelatedDocument = Pick<Doc, '_id' | '_class'>` — KHÔNG có relationType field.

### Mapping relationType → storage (Huly data model)

| Pi-huly API | Huly storage | Action |
|---|---|---|
| `blocks` | `source.relations[]` push `{_id: target, _class}` | `$push` trên Issue nguồn |
| `relates-to` | `source.relations[]` (cùng mảng blocks — KHÔNG phân biệt) | `$push` trên Issue nguồn |
| `is-blocked-by` | `target.blockedBy[]` push `{_id: source, _class}` | `$push` trên Issue ĐÍCH (reverse) |

**Honest limitation**: blocks vs relates-to KHÔNG phân biệt được sau khi lưu
(cùng mảng relations). Tool description + list output note rõ cho LLM.

## Files changed

- `src/tools/domains/issues-relations.ts` — 3 tool add/remove/list refactor inline
- `src/tools/domains/_class-refs.ts` — xóa TS_RELATION_CLASS (dead code)
- `src/tools/domains/__tests__/issues-relations.test.ts` — 13 tests (inline $push/$pull,
  reverse direction, idempotent, early-return)

## Breaking change

`remove_issue_relation` param đổi: `relation: <_id>` → `targetIssue + relationType`
(relation giờ là array element, KHÔNG doc riêng). `$pull` theo `{_id, _class}`.

## Acceptance criteria

- [x] T-58 verify TsRelation inline (CONFIRMED — Issue.relations RelatedDocument[])
- [x] add_issue_relation dùng $push Issue.relations (blocks/relates-to) hoặc target.blockedBy (is-blocked-by)
- [x] list_issue_relations read Issue.relations + blockedBy trực tiếp (KHÔNG findAll)
- [x] remove_issue_relation dùng $pull + early-return khi !existed (code-review M2)
- [x] Idempotent guards (hasRelation check trước push)
- [x] TS_RELATION_CLASS xóa (dead code removed)
- [x] Reality-checker CONFIRMED — KHÔNG còn addCollection(TS_RELATION_CLASS)
