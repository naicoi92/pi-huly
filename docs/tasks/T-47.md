# T-47 — fix(update_issue): status persist + stop auto-assignee leak

> Task spec. Status theo dõi tại [`TASKS.md`](../../TASKS.md).

## Metadata

| Field | Value |
|---|---|
| **ID** | T-47 |
| **Size** | M |
| **Priority** | critical |
| **Blocked by** | (none) — có thể start ngay |
| **Blocks** | (none) |
| **GitHub issue** | [#36](https://github.com/naicoi92/pi-huly/issues/36) |
| **Status** | Done |

## Vấn đề (2 root cause)

`huly_update_issue({ identifier, status })` trả `"Updated issue LST-1: assignee, status"`
(báo success) nhưng `status` **KHÔNG persist** — get_issue ngay sau đó vẫn thấy
giá trị cũ. Đồng thời `assignee` bị auto-set thành current user dù caller KHÔNG
truyền `assignee`.

### Root cause 1 — D15 `needsAssignee` leak sang update

`src/tools/domains/issues-core.ts:230` khai báo `needsAssignee: true` trên
`update_issue`. Builder (D15 FR-18) auto-fill `params.assignee = currentUser.email`
khi absent → assignee bị set dù caller không yêu cầu. Đây là rule cho **create**
(`create_issue` line 132), KHÔNG nên apply cho `update`.

```ts
// issues-core.ts:230 — BUG
defineHulyTool({
  name: "update_issue",
  needsProject: true,
  needsAssignee: true,   // ← LEAK: chỉ create_issue mới cần default này
  ...
```

### Root cause 2 — status không map workflow enum / không verify

`issues-core.ts:266` chỉ `ops.status = params.status` — push raw string `"Done"`
thẳng vào server. Huly lưu status dạng full ref `tracker:status:Done` (get_issue
trả full ref). Có thể server reject transition nhưng tool nuốt error → silent
success. Hoặc status name format sai (short `Done` vs full `tracker:status:Done`).

Hypothesis từ report #36: Huly self-hosted reject status transition nhưng
WebSocket response không có error field, wrapper parse `OK` → return success.

## Phương án

1. **Remove `needsAssignee: true`** khỏi `update_issue` (line 230). Chỉ
   `create_issue` giữ rule D15. Builder không còn auto-fill assignee cho update.
2. **Validate status trước khi gửi server**: dùng `list_statuses` hoặc
   `findOne(ISSUE_STATUS_CLASS, ...)` resolve short name → full ref
   (`tracker:status:Done`) trước khi `ops.status = <full ref>`. Nếu status không
   hợp lệ → `isError: true` + suggest valid statuses.
3. **Read-back verify** (optional, defensive): sau `updateDoc`, `findOne` lại issue
   → assert field đã persist. Nếu mismatch → return warning/error thay vì
   silent success.
4. **Propagate server error**: nếu Huly reject update (WebSocket response có
   error) → throw/return error rõ ràng, KHÔNG parse OK giả.

## Acceptance

- [x] `huly_update_issue({ identifier: "LST-1", status: "Done" })` → status persist,
  `get_issue` đọc lại thấy `tracker:status:Done` — verified: resolve short name →
  full ref (`tracker:status:Done`) trước khi updateDoc (test line 364-391)
- [x] `huly_update_issue({ identifier: "LST-1", title: "..." })` → assignee KHÔNG bị set
  (giữ nguyên giá trị cũ), KHÔNG auto-claim current user — verified: needsAssignee
  removed khỏi update_issue (chỉ create_issue giữ D15) (test line 334-362)
- [x] `huly_update_issue({ identifier, status: "InvalidStatus" })` → `isError: true`
  message list valid statuses — verified (test line 393-420)
- [x] Remove `needsAssignee: true` khỏi `update_issue` (chỉ create_issue giữ) — done line 242
- [x] Test: status map short → full ref; invalid status → error; assignee không leak —
  9 tests cover (assignee leak, valid status, invalid, full ref, no-status skip, empty
  statuses, schema drift, whitespace trim, findAll throw)
- [ ] Test (optional): read-back verify hoặc server error propagation — skipped
  (optional, defensive; status resolve + invalid path đủ cover silent-success root cause)

## Review trail (code-review-mentor × 2 + reality-checker × 1)

- **Round 1** (commit b51d891): 6 issues (0 high, 3 medium, 3 low). Fixed 5
  (#1 empty, #2 schema drift, #3 ambiguity, #5+6 tests); skipped #4 optional read-back.
- **Round 2** (commit c761016): APPROVE. 3 minor (1 🟡 suggestion findAll throw
  uncaught, 2 💭 nits). Fixed 🟡.
- **Round 3** (commit a5453de): catch findAll throw → isError + retry hint. 1 test added.
- **reality-checker integrity**: no fantasy approval. Verified verdict consistent with
  diff (2 random spot-checks). 3 stale docs fixed (TASKS.md test count, T-47.md status
  acceptance checkboxes).
