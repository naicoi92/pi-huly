# T-81 — fix(projects/spaces/components): lead + comments + IssueStatus class + scope

| Field      | Value                       |
| ---------- | --------------------------- |
| Task       | T-81 [L] medium             |
| Issue      | #104                        |
| Blocked-by | (none)                      |
| Blocks     | T-81G (same domain)         |
| Branch     | `fix/T-81-proj-spaces-comp` |

## Vấn đề (reality-checker CONFIRMED)

4 bug. delete_project, delete_component, set_issue_component storage **PASS** —
KHÔNG đụng.

### Bug

- **create_component + update_component** `lead` raw string:
  - `lead: params.lead` lưu raw email/name trực tiếp. `Component.lead` yêu cầu
    `Ref<Employee>`. Trusted: `findPersonByEmailOrName → toRef<Employee>`.
  - Ref rác → UI không resolve lead.
- **create_component** thiếu `comments: 0`:
  - Trusted **luôn** set `comments: 0` (pattern chung Component/Milestone/IssueTemplate).
  - Pi-huly omit → risk server reject hoặc `undefined`.
- **getProjectStatuses** (_common.ts helper) sai class query:
  - Query `tracker:class:IssueStatus` (findOne per ref, N+1).
  - Trusted resolve qua **`core.class.Status`** + model lookup + batch `$in`, comment
    rõ: _"This avoids querying IssueStatus directly which can fail on some workspaces"_
    (issues-shared.ts:314).
  - Pi-huly làm đúng cái trusted cố né → latent failure.
- **component lookups** (get/update/delete/set_issue_component) không space-scope:
  - `findOne(COMPONENT_CLASS, {_id})` không filter `space: project._id`.
  - Trusted scope mọi component lookup theo `project._id`. Phá project isolation.

### PASS (xác nhận đúng)

- delete_project: removeDoc + orphan children intentional (match trusted, +guard)
- delete_component: removeDoc (+guard)
- set_issue_component storage: inline `component` single Ref (KHÔNG collection) — match trusted
- SPACE_CLASS = core:class:Space ✓
- PROJECT_TYPE_CLASS = task:class:ProjectType ✓
- create_component space scoping (project.space = project._id) ✓

## Fix

### `components.ts`

- create_component + update_component: resolve `lead` → `Ref<Employee>` qua
  `findPersonByEmailOrName` (dùng chung contacts.ts helper)
- create_component: thêm `comments: 0` vào createDoc data
- get/update/delete_component + set_issue_component: thêm `space: project._id`
  vào component findOne

### `_common.ts` getProjectStatuses

- Migrate sang `core:class:Status` + batch `$in` query (port trusted pattern từ
  issues-shared.ts). KHÔNG findOne per IssueStatus ref.

## Tests

- create_component: lead resolve Person._id, comments:0 present
- update_component: lead resolve
- getProjectStatuses: core.class.Status query (KHÔNG IssueStatus)
- component scoping: findOne có space:project._id filter

## Verification

- Tests pass (target +~6), lint/typecheck/fmt/md green, CI green

## Residual risks

- lead resolve phụ thuộc findPersonByEmailOrName (email path T-82G)
- core.class.Status migration: verify status name resolution không break list_statuses (T-71)
- Component class ref đã đúng (T-65 area) — chỉ query pattern sai
