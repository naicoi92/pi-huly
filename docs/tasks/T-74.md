# T-74 — account-client: log_time fix + list_workspaces/members honest-unavailable

| Field      | Value                                                                    |
| ---------- | ------------------------------------------------------------------------ |
| Task       | T-74 [L] high (split: sub-slice 1 done, sub-slice 2 deferred behind ADR) |
| Issue      | #82                                                                      |
| Blocked-by | (none)                                                                   |
| Blocks     | T-75                                                                     |
| Branch     | `fix/T-74-account-client`                                                |

## Vấn đề (reality-checker CONFIRMED + scope re-split)

- log_time: collection "timetracking" (must "reports"), value minutes (must hours,
  fractional), missing date/employee.
- list_workspaces: wrong-function (findAll Person, must AccountClient.getUserWorkspaces).
- list_workspace_members: findAll Employee no roles (must AccountClient.getWorkspaceMembers).
- list_employees: email field doesn't exist (always undefined).

reality-checker scope re-split:

- Sub-slice 1a (log_time): data-client only, ship now.
- Sub-slice 1b (list_employees email): data-client, cheap.
- Sub-slice 2 (list_workspaces + list_workspace_members): account-client HTTP layer,
  needs ADR. Honest-unavailable.

## Fix

### log_time (time.ts) — sub-slice 1a

- collection "reports" (Issue.reports field, KHÔNG "timetracking").
- value Type.Number (hours, fractional — 0.25=15min, 8=1day).
- date: Date.now().
- employee: best-effort resolve (findOne Employee {personUuid: currentUser.id}).
- Removed dead needsAssignee/assigneeField "user".

### list_employees (contacts.ts) — sub-slice 1b

- Drop email field (KHÔNG tồn tại trên Employee/Person — lives trong Channel).
  Was always undefined. name reliable.

### list_workspaces + list_workspace_members (workspace.ts) — sub-slice 2

- honest-unavailable: reason=account_client_layer_required.
- list_workspace_members suggests huly_list_employees alternative.

## Deferred (behind ADR)

- Add @hcengineering/account-client dep + WorkspaceClient wrapper (HTTP).
- list_workspaces → AccountClient.getUserWorkspaces.
- list_workspace_members → AccountClient.getWorkspaceMembers (+ roles).

## Tests

- t77 test +1: log_time collection "reports" + hours + date.
- workspace.test.ts: list_workspaces/members → honest-unavailable (rewrite).

## Verification

- 644 tests pass
- lint/typecheck/fmt/md green

## Residual risks

- employee resolve best-effort (findOne by personUuid) — if Employee schema differs,
  omit (server may default).
- Sub-slice 2 (account-client) deferred — 2 tools honest-unavailable until ADR.
- Self-host verify deferred T-53.
