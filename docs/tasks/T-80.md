# T-80 — fix(issues read-path): raw refs + broken blocks query + assignee resolve

| Field      | Value                            |
| ---------- | -------------------------------- |
| Task       | T-80 [M] high                    |
| Issue      | #103                             |
| Blocked-by | (none; unblocked-by T-82G email) |
| Blocks     | (none)                           |
| Branch     | `fix/T-80-issues-readpath`       |

## Vấn đề (reality-checker CONFIRMED)

Issues **read-path** bugs. Relations (add/remove/storage T-61) + move_issue
(T-68) **PASS** — KHÔNG đụng. 3 tool read/resolve sai.

### Bug

- **get_issue** thiếu fields + raw refs:
  - Thiếu: `labels[]`, `parentIssue` (direct parent identifier), `subIssues` count,
    `modifiedOn`/`createdOn`
  - `status`: trả raw `_id` ref (vd `tracker:status:Done`) — trusted resolve
    `resolveStatusName(statuses, issue.status)` → name ("Done"). LLM nhận opaque ref.
  - `assignee`: trả raw Person `_id` — trusted `findOne(Person, _id)` → `person.name`.
- **list_issue_relations** 2 bug:
  - Trả `targetIssueId` = raw `_id` Ref (KHÔNG identifier). Trusted batch
    `findAll({_id:{$in}})` → `idToIdentifier` map → mỗi entry `{identifier, _id, _class}`.
  - **Broken blocks query**: `{ "blockedBy._id": issue._id }` — dotted-path vào array
    element returns NO rows trong Huly query engine. Trusted comment (PR #48
    verification): _"querying `{ "blockedBy._id": issue._id }` returns no rows"_.
    Phải `{ blockedBy: { _id: issue._id, _class: ISSUE_CLASS } }`. → category `blocks`
    LUÔN rỗng hiện tại.
- **update_issue** assignee raw push:
  - `ops.assignee = params.assignee` (raw email/name string) → lưu garbage vào
    `Ref<Person>`. Trusted `resolveAssignee → person._id`. Inconsistent với
    create_issue (dùng `findPersonByEmailOrName` resolve đúng).
  - Không hỗ trợ `null` (unassign).

### PASS (xác nhận đúng, KHÔNG sửa)

- delete_issue: removeDoc đơn lẻ, KHÔNG cascade (match trusted — Huly không cascade
  mặc định, children orphan intentional cả 2 impl)
- add_issue_relation (T-61 storage): blocks→target.blockedBy, is-blocked-by→source.blockedBy,
  relates-to→bidirectional. `makeRelatedDoc` shape match 100%
- remove_issue_relation: symmetric $pull
- move_issue (T-68): attachedTo/parents/subIssues inc-dec match trusted

## Fix

### `issues-core.ts` get_issue

- Resolve status→name (qua `getProjectStatuses` helper — note: T-81 fix IssueStatus
  class query, dùng sau)
- Resolve assignee→Person name (`findOne(PERSON_CLASS, {_id: assignee})`)
- Add fields: labels (findAll TagReference attachedTo), parentIssue (parents[last].identifier),
  subIssues (count), modifiedOn, createdOn

### `issues-relations.ts` list_issue_relations

- Batch findAll resolve `_id`→`identifier` (port trusted `idToIdentifier`)
- Blocks query: `{ blockedBy: { _id: issue._id, _class: ISSUE_CLASS } }` (KHÔNG dotted)

### `issues-core.ts` update_issue

- assignee resolve Person (`findPersonByEmailOrName`) trước push (đồng bộ create_issue)
- `assignee: Type.Union([Type.String(), Type.Null()])` + branch null → `ops.assignee = null`

## Tests

- get_issue: status→name, assignee→name, labels/parentIssue/subIssues/modifiedOn
- list_issue_relations: identifier resolve (KHÔNG raw _id), blocks query returns rows
- update_issue: assignee resolve Person._id, null clear

## Verification

- Tests pass (target +~7), lint/typecheck/fmt/md green, CI green

## Residual risks

- assignee resolve phụ thuộc `findPersonByEmailOrName` — email path deferred (T-82G
  unblocks full email lookup; hiện chỉ name-match work)
- get_issue assignee Person lookup = extra findOne (N+1 risk nếu batch — acceptable
  single issue)
